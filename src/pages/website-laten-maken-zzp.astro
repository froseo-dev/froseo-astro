---
/**
 * SEO-landingspagina /website-laten-maken-zzp/.
 *
 * Focus: zzp-zoekintentie. Custom Astro-page zodat we vrij blokken kunnen
 * schikken en achtergrondkleuren afwisselen voor visueel ritme.
 *
 * Sectievolgorde:
 *   1. Hero (cream, standaard service-hero-patroon)
 *   2. LogosTicker
 *   3. Echte <h1> + intro-lead (cream, compact)
 *   4. Pakketten + advies-CTA samen (donkerpaars + grid)
 *   5. Showcase (cases)
 *   6. USPs - 'Wat we standaard regelen' (cream)
 *   7. Wanneer is het de investering waard? (cream)
 *   8. Comparison 'Zelf vs laten maken?' + CTA-afsluiter
 *   9. Branches - 4 grouped cards (cream)
 *  10. BetterCallCalvin
 *  11. FAQ
 *
 * Pagina is bewust niet in het hoofdmenu opgenomen.
 */

import BaseLayout from '@layouts/BaseLayout.astro';
import Topbar from '@components/layout/Topbar.astro';
import Nav from '@components/layout/Nav.astro';
import Footer from '@components/layout/Footer.astro';
import EyebrowPill from '@components/ui/EyebrowPill.astro';
import ReviewAvatars from '@components/ui/ReviewAvatars.astro';
import SectionIcon from '@components/ui/SectionIcon.astro';
import LogosTicker from '@components/sections/LogosTicker.astro';
import WebsitesShowcase from '@components/sections/WebsitesShowcase.astro';
import BetterCallCalvin from '@components/sections/BetterCallCalvin.astro';
import Comparison from '@components/sections/Comparison.astro';
import FAQ from '@components/sections/FAQ.astro';
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import { review, pricing } from '@data/site-config';
import heroMockup from '@assets/hero/website-laten-maken-zzp-hero.webp';

const pageTitle = "Website laten maken als zzp'er | Vanaf €89/m | Froseo";
const pageDesc =
  `Laat als zzp'er een professionele website maken die snel laadt, vertrouwen wekt en klaar is voor Google. Kies maatwerk of een all-in website abonnement vanaf ${pricing.abboFromInline}.`;

const allCases = await getCollection('cases', ({ data }) => data.published !== false);
const showcaseSlugs = [
  'renovatie-totaal',
  'club12',
  'prime-performance',
  'steunder',
  'studio-contenido',
  'theo-mackaay',
];
const showcaseCases = showcaseSlugs
  .map((slug) => allCases.find((c) => c.data.slug === slug))
  .filter((c): c is NonNullable<typeof c> => Boolean(c));

const priceOptions = [
  {
    name: 'Website abonnement',
    forWhom: 'Geen investering vooraf, alles geregeld',
    price: `Vanaf ${pricing.abboFromInline}`,
    description: "Een professionele site voor een vast maandbedrag. Hosting, technisch beheer en kleine wijzigingen zitten erbij. Geen opstartkosten, maandelijks opzegbaar na drie maanden.",
    cta: { label: 'Bekijk het abonnement', href: '/website-abonnement/' },
    featured: true,
  },
  {
    name: 'One-pager website',
    forWhom: "Startende zzp'er, professioneel online",
    price: 'Vanaf €499',
    description: "Een strakke, snelle one-pager die professioneel overkomt zodra iemand je naam googelt. Modern design, gericht op conversie.",
    cta: { label: 'Vraag offerte aan', href: '/contact/?onderwerp=zzp-onepager' },
  },
  {
    name: 'Maatwerk website',
    forWhom: "Zzp'er met meerdere diensten of lokale ambitie",
    price: 'Vanaf €999',
    description: "Maatwerk met aparte dienstenpagina's, sterke structuur en lokale SEO-basis. Klaar om mee te groeien naarmate je bedrijf groeit.",
    cta: { label: 'Vraag offerte aan', href: '/contact/?onderwerp=zzp-maatwerk' },
  },
];

const investmentWorth = [
  'Klanten je eerst online checken voordat ze contact opnemen.',
  'Je afhankelijk bent van aanvragen uit je regio.',
  'Je niet alleen via-via gevonden wilt worden.',
  'Je je diensten beter wilt uitleggen dan op social media past.',
  'Je betrouwbaar wilt overkomen naast je concurrenten.',
  'Je Google wilt gebruiken als structureel kanaal voor nieuwe klanten.',
];

const usps = [
  {
    icon: 'shieldCheck',
    title: 'Google Proof',
    body: "Elke site wordt Google Proof opgeleverd, klaar om lokaal gevonden te worden. Goede techniek, snelle pagina's en de juiste structuur zitten er vanaf de basis in.",
  },
  {
    icon: 'locationPin',
    title: 'Lokaal vindbaar',
    body: "Als zzp'er richt je je vaak op je eigen regio. We zorgen dat je in jouw plaats en branche bovenaan in Google opduikt.",
  },
  {
    icon: 'paletteSwatch',
    title: 'Huisstijl en design',
    body: 'Je logo en huisstijl horen bij je online identiteit. Hulp nodig met het ontwerp? We denken graag mee.',
  },
  {
    icon: 'toolbox',
    title: 'Full-service',
    body: 'Bedrijfs-e-mail, hosting, technisch beheer en onderhoud: we regelen het allemaal. Jij blijft je werk doen, wij houden je site scherp.',
  },
];

const comparison = {
  title: 'Zelf maken of je zzp-website',
  titleAccent: 'laten maken?',
  withoutLabel: 'Zelf maken',
  withLabel: 'Door Froseo laten maken',
  items: [
    { without: 'Goedkoop starten', with: 'Vanaf dag één professioneel oogt' },
    { without: 'Veel zelf uitzoeken en aanmodderen', with: 'Wij regelen het, jij houdt tijd over voor je werk' },
    { without: 'Prima voor een simpele start', with: 'Betere basis voor SEO en conversie' },
    { without: 'Vaak beperkt maatwerk', with: 'Flexibeler uit te breiden naarmate je groeit' },
    { without: 'Kost vooral je eigen tijd', with: 'Kost geld, maar bespaart tijd en gedoe' },
  ],
};

/* Branches - vier gegroepeerde categorieen. */
const segments = [
  {
    title: 'Vakmensen',
    branches: 'Dakdekkers, aannemers, loodgieters, schilders, stukadoors, installateurs, schoorsteenvegers, timmermannen, elektriciens, tuinmannen en hoveniers.',
    body: "Snelle, mobiel-eerst sites met offerteformulieren, een klikbaar telefoonnummer en lokale SEO. Klanten zoeken vakmensen vaak met spoed en op de telefoon.",
  },
  {
    title: 'Gezondheid en coaching',
    branches: "Personal trainers, coaches, yogastudio's en (massage)therapeuten.",
    body: 'Sites die rust uitstralen, je aanpak en aanbod helder tonen en bezoekers een laagdrempelige intake aanbieden. Vertrouwen wekken is hier het halve werk.',
  },
  {
    title: 'Beauty en lifestyle',
    branches: 'Kappers, nagelstylisten, tattoo artists en schoonheidsspecialisten.',
    body: "Visuele sites die je werk laten spreken: strakke portfolio, reviews en een eenvoudige boekingsoptie. Klanten beslissen vaak binnen seconden op mobiel.",
  },
  {
    title: 'Creatief en dienstverlening',
    branches: 'Fotografen, ontwerpers, consultants en kleine bureaus.',
    body: "Sites met sterke beeldtaal, duidelijke dienstenpagina's en ruimte voor je verhaal en je werk. Past bij zzp'ers waar de eerste indruk telt.",
  },
];

const faqs = [
  {
    q: "Wat kost een website laten maken als zzp'er?",
    a: `Dat hangt af van wat je nodig hebt. Een one-pager begint rond de €499, een maatwerk-website vanaf €999. Wil je geen investering vooraf, dan kun je kiezen voor het website abonnement vanaf ${pricing.abboFromInline}, inclusief hosting en onderhoud. In een vrijblijvend gesprek bespreken we welke optie past en geven we vooraf een heldere prijs.`,
  },
  {
    q: "Kan ik als zzp'er ook een website abonnement nemen?",
    a: `Ja, juist voor zzp'ers is dat vaak de slimste route. Geen grote investering vooraf, een vast bedrag per maand en hosting, onderhoud en kleine wijzigingen zitten erbij. Het abonnement start vanaf ${pricing.abboFromInline} en is maandelijks opzegbaar na drie maanden.`,
  },
  {
    q: 'Wat is het verschil tussen een goedkope en een professionele zzp-website?',
    a: "Voor een paar honderd euro kun je ergens een site laten bouwen. Prima als je alleen een digitaal visitekaartje nodig hebt. Wil je professioneel overkomen, lokaal gevonden worden en later kunnen uitbreiden, dan is de basis belangrijker dan alleen de laagste prijs. Denk aan snelheid, mobiele ervaring, structuur, goede teksten en structureel onderhoud.",
  },
  {
    q: "Is een one-page website genoeg voor een zzp'er?",
    a: 'Vaak wel. Als je vooral professioneel wilt overkomen wanneer iemand je naam googelt, of een aanvraag mogelijk wilt maken, dan is een goed gebouwde one-pager prima. Wil je op meerdere diensten of plaatsen lokaal gevonden worden, dan is een uitgebreidere site met aparte pagina\'s slimmer.',
  },
  {
    q: "Kan mijn website later worden uitgebreid met SEO-pagina's?",
    a: "Zeker. We bouwen flexibel: starten met een one-pager of compacte site kan, en later breiden we uit met dienst- of locatiepagina's zodra je lokaal beter gevonden wilt worden. Geen opnieuw beginnen, gewoon doorbouwen op dezelfde basis.",
  },
  {
    q: "Maken jullie ook websites voor startende zzp'ers?",
    a: "Ja. Veel startende zzp'ers kiezen voor het abonnement of een compacte one-pager: snel professioneel online zonder grote investering vooraf. We denken mee over wat je wel en nog niet nodig hebt.",
  },
  {
    q: 'Is WordPress of Astro beter voor mijn zzp-website?',
    a: 'Dat hangt af van wat je site moet doen. WordPress is fijn als je vaak zelf content wilt aanpassen en bloggen. Astro is razendsnel en ideaal voor sites die vooral moeten presteren in Google en op mobiel. We adviseren wat past bij jouw situatie en doelen.',
  },
  {
    q: "Word ik goed gevonden in Google als zzp'er?",
    a: 'Jazeker. We bouwen je site met lokale SEO als basis. Dat betekent dat je goed scoort op zoekopdrachten als "loodgieter Utrecht" of "personal trainer Haarlem". We zorgen voor een sterke technische basis, snelle laadtijd en koppeling met je Google Business-profiel.',
  },
  {
    q: 'Ik heb weinig tijd. Hoeveel moet ik zelf doen?',
    a: "Je levert wat basisinformatie aan (diensten, logo, eventueel wat foto's), wij doen de rest: ontwerp, techniek, teksten en inrichting. Geen dagenlang puzzelen of zelf alles schrijven.",
  },
  {
    q: 'Regelen jullie ook hosting en e-mail?',
    a: 'Ja. Domeinnaam, snelle hosting en professionele e-mailadressen (zoals info@jouwbedrijf.nl) regelen we. Wil je je mail koppelen aan Gmail of Outlook? Ook dat doen we voor je.',
  },
  {
    q: 'Ik heb al werk via-via. Heb ik dan wel een website nodig?',
    a: 'Ook dan is een site slim. Veel mensen kijken eerst online of iemand betrouwbaar overkomt voordat ze contact opnemen. Daarnaast helpt het je voorbereid te zijn op rustigere periodes.',
  },
];
---

<BaseLayout title={pageTitle} description={pageDesc}>
  <Topbar />
  <Nav />
  <main>
    <article class="zzp-page">

      <!-- ============================================================
           1. HERO - standaard service-hero-patroon.
           ============================================================ -->
      <section class="service-hero has-image">
        <div class="service-hero-inner">
          <div class="service-hero-text">
            <EyebrowPill>Voor zzp'ers</EyebrowPill>
            <div class="service-hero-title">
              Betaalbare websites <em>voor ZZP'ers.</em>
            </div>
            <ul class="service-hero-checks">
              <li>Maatwerk of all-in website abonnement vanaf {pricing.abboFromShort}</li>
              <li>Sterke structuur, snelle techniek en lokale SEO-basis</li>
              <li>Wij regelen design, teksten, hosting en onderhoud</li>
            </ul>
            <div class="service-hero-actions">
              <a class="btn-primary" href="/contact/?onderwerp=zzp-website">Bespreek mijn zzp-website ↗</a>
              <a class="btn-ghost" href="#cases">Bekijk voorbeelden ↓</a>
            </div>
            <div class="service-hero-reviews reviews-inline">
              <ReviewAvatars />
              <div class="stars">★★★★★</div>
              <span><strong>{review.ratingDisplay}</strong> · Google reviews</span>
            </div>
          </div>

          <div class="service-hero-media">
            <Image
              src={heroMockup}
              alt="Website laten maken voor zzp'er door Froseo: Steunder op desktop, Prime Performance op mobiel"
              title="Website laten maken voor zzp'er door Froseo: Steunder op desktop, Prime Performance op mobiel"
              class="service-hero-img"
              widths={[640, 960, 1280, 1600]}
              sizes="(max-width: 1024px) 100vw, 640px"
              loading="eager"
              fetchpriority="high"
            />
          </div>
        </div>
      </section>

      <LogosTicker />

      <!-- ============================================================
           2. ECHTE <h1> + intro-lead.
           ============================================================ -->
      <section class="zzp-intro">
        <div class="zzp-intro-inner">
          <EyebrowPill variant="rare">Voor zzp'ers</EyebrowPill>
          <h1 class="zzp-intro-h1">Website laten maken als <em>zzp'er</em></h1>
          <p class="zzp-intro-lead">
            Je bent hier waarschijnlijk omdat je een site wilt die professioneel oogt, klanten oplevert en niet te veel gedoe geeft. We bouwen sites die daar precies op gericht zijn: snel, vindbaar en passend bij wat jij doet. Geen vage bureau-praat, wel een eerlijk gesprek over wat je echt nodig hebt.
          </p>
        </div>
      </section>

      <!-- ============================================================
           3. PAKKETTEN + ADVIES-CTA - samen op donkerpaars + grid.
           ============================================================ -->
      <section class="zzp-deal">
        <div class="zzp-deal-inner">
          <header class="zzp-deal-head">
            <EyebrowPill variant="highlight">Drie routes</EyebrowPill>
            <h2 class="zzp-deal-title">Wat kost een website laten maken als <em>zzp'er?</em></h2>
            <p class="zzp-deal-lead">
              Welke route slim is, hangt af van waar je staat. Geen investering vooraf? Het abonnement. Betaalbaar professioneel online? Een one-pager. Echt lokaal gevonden worden op meerdere diensten? Maatwerk.
            </p>
          </header>
          <div class="zzp-deal-grid">
            {priceOptions.map((opt) => (
              <article class={`zzp-price-card${opt.featured ? ' is-featured' : ''}`}>
                {opt.featured && <span class="zzp-price-badge">Meest gekozen</span>}
                <h3 class="zzp-price-name">{opt.name}</h3>
                <p class="zzp-price-for">{opt.forWhom}</p>
                <div class="zzp-price-amount">{opt.price}</div>
                <p class="zzp-price-desc">{opt.description}</p>
                <a class={opt.featured ? 'btn-primary' : 'btn-ghost'} href={opt.cta.href}>{opt.cta.label} ↗</a>
              </article>
            ))}
          </div>

          <!-- Advies-CTA binnen dezelfde donkere sectie. -->
          <div class="zzp-deal-cta">
            <h3 class="zzp-deal-cta-title">Twijfel je welke route bij je past?</h3>
            <p class="zzp-deal-cta-body">
              Stuur even wat je hebt staan of waar je naar zoekt, dan denken we vrijblijvend mee en geven we eerlijk advies.
            </p>
            <a class="btn-primary" href="/contact/?onderwerp=zzp-website-advies">Vraag vrijblijvend advies aan ↗</a>
          </div>
        </div>
      </section>

      <!-- ============================================================
           4. CASES - showcase.
           ============================================================ -->
      <div id="cases">
        <WebsitesShowcase
          cases={showcaseCases}
          eyebrow="Voorbeelden"
          title="Websites die we maakten voor"
          titleAccent="zzp'ers en kleine bedrijven."
        />
      </div>

      <!-- ============================================================
           5. USPs - 'Wat we standaard regelen'.
           ============================================================ -->
      <section class="zzp-usps">
        <div class="zzp-usps-inner">
          <header class="zzp-usps-head">
            <EyebrowPill variant="rare">Wat we regelen</EyebrowPill>
            <h2 class="zzp-usps-title">Wat we voor ZZP'ers <em>standaard regelen.</em></h2>
          </header>
          <div class="zzp-usps-grid">
            {usps.map((u) => (
              <article class="zzp-usp-card">
                <SectionIcon name={u.icon} class="zzp-usp-icon" size={40} />
                <h3 class="zzp-usp-title">{u.title}</h3>
                <p class="zzp-usp-body">{u.body}</p>
              </article>
            ))}
          </div>
          <div class="zzp-section-cta">
            <a class="btn-primary" href="/contact/?onderwerp=zzp-website">Bespreek je zzp-website ↗</a>
          </div>
        </div>
      </section>

      <!-- ============================================================
           6. Wanneer is het de investering waard? (cream, met checks).
           ============================================================ -->
      <section class="zzp-worth">
        <div class="zzp-worth-inner">
          <header class="zzp-worth-head">
            <EyebrowPill>Wanneer wel, wanneer niet</EyebrowPill>
            <h2 class="zzp-worth-title">Wanneer is een zzp-website de <em>investering waard?</em></h2>
          </header>
          <ul class="zzp-worth-list">
            {investmentWorth.map((item) => (
              <li><span class="zzp-worth-check" aria-hidden="true">✓</span><span>{item}</span></li>
            ))}
          </ul>
          <p class="zzp-worth-after">
            Voor sommige zzp'ers is een digitaal visitekaartje al genoeg. Voor anderen is de website het belangrijkste verkoopkanaal. Daarom bouwen we niet één standaardpakket voor iedereen, maar kijken we naar wat jouw bedrijf nodig heeft.
          </p>
          <div class="zzp-section-cta">
            <a class="btn-primary" href="/contact/?onderwerp=zzp-website-advies">Plan een vrijblijvend gesprek ↗</a>
          </div>
        </div>
      </section>

      <!-- ============================================================
           7. COMPARISON - zelf maken vs laten maken + CTA-afsluiter.
           ============================================================ -->
      <Comparison
        title={comparison.title}
        titleAccent={comparison.titleAccent}
        withoutLabel={comparison.withoutLabel}
        withLabel={comparison.withLabel}
        items={comparison.items}
      />
      <section class="zzp-compare-after">
        <div class="zzp-compare-after-inner">
          <p>
            Twijfel je nog? We denken eerlijk mee. Soms is zelf starten prima. Maar als je site klanten moet overtuigen of lokaal vindbaar moet worden, is professioneel laten maken meestal slimmer.
          </p>
          <a class="btn-ghost" href="/contact/?onderwerp=zzp-website-advies">Stel je vraag ↗</a>
        </div>
      </section>

      <!-- ============================================================
           8. BRANCHES - 4 gegroepeerde cards (clean markup).
           ============================================================ -->
      <section class="zzp-segments">
        <div class="zzp-segments-inner">
          <header class="zzp-segments-head">
            <EyebrowPill variant="rare">Voor welke zzp'ers</EyebrowPill>
            <h2 class="zzp-segments-title">Voor welke ZZP'ers maken we <em>websites?</em></h2>
            <p class="zzp-segments-lead">
              Eigenlijk voor elke zzp'er die professioneel online wil zijn. Een greep uit ons werk:
            </p>
          </header>
          <div class="zzp-segments-grid">
            {segments.map((s) => (
              <article class="zzp-segment-card">
                <h3>{s.title}</h3>
                <p class="zzp-segment-body">{s.body}</p>
                <p class="zzp-segment-branches"><strong>Bijvoorbeeld:</strong> {s.branches}</p>
              </article>
            ))}
          </div>
          <div class="zzp-section-cta">
            <a class="btn-primary" href="/contact/?onderwerp=zzp-website">Vraag een offerte aan ↗</a>
            <a class="btn-ghost" href="/portfolio/">Bekijk ons portfolio ↗</a>
          </div>
        </div>
      </section>

      <BetterCallCalvin
        eyebrow="Tijd voor een kennismaking?"
        titleLine1="Plan een korte"
        titleAccent="kennismaking."
        body={[
          'Een mailtje, een WhatsApp of een gesprek van 30 minuten: jij bepaalt hoe laagdrempelig we beginnen.',
          'Geheel vrijblijvend. Past het niet, dan zeggen we dat eerlijk.',
        ]}
        primaryLabel="Plan een afspraak ↗"
        primaryHref="/contact/?onderwerp=zzp-website"
      />
    </article>

    <FAQ
      eyebrow="Veelgestelde vragen"
      titleLine1="Vragen over een website laten maken als"
      titleAccent="zzp'er."
      items={faqs}
    />
  </main>
  <Footer />
</BaseLayout>

<style>
  /* ============================================================
     Algemene CTA-rij die we onder secties hergebruiken.
     ============================================================ */
  .zzp-section-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    justify-content: center;
    margin-top: clamp(1.5rem, 3vw, 2.25rem);
  }

  /* ============================================================
     Intro - echte H1 + lead.
     ============================================================ */
  .zzp-intro { padding: clamp(2.5rem, 5vw, 4rem) var(--gutter, 1.5rem) clamp(1.5rem, 3vw, 2rem); }
  .zzp-intro-inner {
    max-width: 60rem;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .zzp-intro-h1 {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.9rem, 3.6vw, 2.8rem);
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
  }
  .zzp-intro-h1 em { color: var(--accent-primary); font-style: normal; }
  .zzp-intro-lead {
    line-height: 1.65;
    color: var(--text-muted, #555);
    max-width: 50rem;
    margin: 0;
  }

  /* ============================================================
     Pakketten + advies-CTA - donkerpaars + grid (zelfde patroon
     als de LP-vergelijking en het 'wanneer waard'-blok eerder).
     ============================================================ */
  .zzp-deal {
    background-color: var(--color-purple-strong, #3B006D);
    background-image:
      radial-gradient(ellipse at center, transparent 35%, var(--color-purple-strong, #3B006D) 92%),
      repeating-linear-gradient(0deg, rgba(255, 255, 255, 0.05) 0 1px, transparent 1px 60px),
      repeating-linear-gradient(90deg, rgba(255, 255, 255, 0.05) 0 1px, transparent 1px 60px);
    background-size: cover, 60px 60px, 60px 60px;
    background-repeat: no-repeat, repeat, repeat;
    color: #fff;
    padding: clamp(3.5rem, 6vw, 5rem) var(--gutter, 1.5rem);
  }
  .zzp-deal-inner { max-width: var(--container-max, 1200px); margin: 0 auto; }
  .zzp-deal-head {
    text-align: center;
    max-width: 56rem;
    margin: 0 auto 2.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .zzp-deal-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.9rem, 3.5vw, 2.8rem);
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
    color: #fff;
  }
  .zzp-deal-title em { color: var(--accent-highlight); font-style: normal; }
  .zzp-deal-lead { line-height: 1.65; opacity: 0.9; margin: 0; max-width: 50rem; }
  .zzp-deal-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    align-items: stretch;
  }
  .zzp-price-card {
    background: var(--surface-card, #fff);
    border: 1px solid rgba(0, 0, 0, 0.06);
    border-radius: var(--radius-lg, 14px);
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.18);
    position: relative;
    color: var(--text-primary, #1a1a1a);
    transition: transform 0.18s ease;
  }
  .zzp-price-card:hover { transform: translateY(-3px); }
  .zzp-price-card.is-featured { border-color: var(--accent-primary); }
  .zzp-price-badge {
    position: absolute;
    top: -0.65rem;
    left: 1.25rem;
    background: var(--accent-highlight);
    color: var(--surface-dark, #1a0033);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    padding: 0.28rem 0.7rem;
    border-radius: 999px;
  }
  .zzp-price-name {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: 1.25rem;
    margin: 0 0 0.2rem;
    color: var(--text-primary, #1a1a1a);
  }
  .zzp-price-for { font-size: 0.86rem; color: var(--text-muted, #666); margin: 0 0 1rem; }
  .zzp-price-amount {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: 1.5rem;
    color: var(--accent-primary);
    margin-bottom: 1rem;
  }
  .zzp-price-desc {
    line-height: 1.6;
    flex-grow: 1;
    margin-bottom: 1.25rem;
    font-size: 0.94rem;
    color: var(--text-primary, #1a1a1a);
  }
  .zzp-price-card .btn-primary,
  .zzp-price-card .btn-ghost { align-self: flex-start; }

  /* Advies-CTA onderin de dark sectie. */
  .zzp-deal-cta {
    margin-top: clamp(2.5rem, 5vw, 3.5rem);
    text-align: center;
    max-width: 44rem;
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
    padding-top: clamp(2rem, 4vw, 3rem);
    border-top: 1px solid rgba(255, 255, 255, 0.15);
  }
  .zzp-deal-cta-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.3rem, 2.4vw, 1.7rem);
    line-height: 1.2;
    margin: 0;
    color: #fff;
  }
  .zzp-deal-cta-body { opacity: 0.88; line-height: 1.6; margin: 0; }

  @media (max-width: 900px) {
    .zzp-deal-grid { grid-template-columns: 1fr; }
  }

  /* ============================================================
     USPs - 4 cards (cream).
     ============================================================ */
  .zzp-usps { padding: clamp(3rem, 5vw, 4.5rem) var(--gutter, 1.5rem); }
  .zzp-usps-inner { max-width: var(--container-max, 1200px); margin: 0 auto; }
  .zzp-usps-head {
    text-align: center;
    max-width: 50rem;
    margin: 0 auto 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
  }
  .zzp-usps-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.8rem, 3.3vw, 2.5rem);
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
  }
  .zzp-usps-title em { color: var(--accent-primary); font-style: normal; }
  .zzp-usps-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; }
  .zzp-usp-card {
    background: var(--surface-card, #fff);
    border: 1px solid rgba(0, 0, 0, 0.06);
    border-radius: var(--radius-lg, 14px);
    padding: 1.5rem;
  }
  .zzp-usp-icon { color: var(--accent-primary); margin-bottom: 0.75rem; }
  .zzp-usp-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: 1.05rem;
    margin: 0 0 0.5rem;
  }
  .zzp-usp-body { line-height: 1.55; font-size: 0.92rem; }
  @media (max-width: 1000px) { .zzp-usps-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 540px) { .zzp-usps-grid { grid-template-columns: 1fr; } }

  /* ============================================================
     Wanneer is het waard? - cream centered met check-lijst.
     ============================================================ */
  .zzp-worth { padding: clamp(3rem, 5vw, 4.5rem) var(--gutter, 1.5rem); }
  .zzp-worth-inner {
    max-width: 50rem;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .zzp-worth-head {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
    margin-bottom: 1.5rem;
  }
  .zzp-worth-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.8rem, 3.3vw, 2.5rem);
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
  }
  .zzp-worth-title em { color: var(--accent-primary); font-style: normal; }
  .zzp-worth-list {
    list-style: none;
    padding: 0;
    margin: 0 auto 1.5rem;
    max-width: 40rem;
    text-align: left;
    display: grid;
    gap: 0.7rem;
    width: 100%;
  }
  .zzp-worth-list li {
    display: grid;
    grid-template-columns: 1.5rem 1fr;
    gap: 0.6rem;
    align-items: start;
    line-height: 1.55;
  }
  .zzp-worth-check {
    display: grid;
    place-items: center;
    width: 1.4rem;
    height: 1.4rem;
    background: var(--accent-primary);
    color: #fff;
    border-radius: 50%;
    font-weight: 800;
    font-size: 0.8rem;
  }
  .zzp-worth-after {
    line-height: 1.6;
    color: var(--text-muted, #555);
    max-width: 44rem;
    margin: 0 auto;
  }

  /* ============================================================
     Comparison-afsluiter (kort tekstje + CTA na de tabel).
     ============================================================ */
  .zzp-compare-after { padding: 0 var(--gutter, 1.5rem) clamp(2.5rem, 5vw, 4rem); }
  .zzp-compare-after-inner {
    max-width: 50rem;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
  }
  .zzp-compare-after-inner p {
    line-height: 1.65;
    color: var(--text-muted, #555);
    margin: 0;
  }

  /* ============================================================
     Branches/segments - 4 gegroepeerde cards. Robuuste flex-reset
     zodat eventuele globale article/p-regels niet interfereren.
     ============================================================ */
  .zzp-segments { padding: clamp(3rem, 5vw, 4.5rem) var(--gutter, 1.5rem); }
  .zzp-segments-inner { max-width: var(--container-max, 1200px); margin: 0 auto; }
  .zzp-segments-head {
    text-align: center;
    max-width: 56rem;
    margin: 0 auto 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.85rem;
  }
  .zzp-segments-title {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: clamp(1.8rem, 3.3vw, 2.5rem);
    line-height: 1.15;
    letter-spacing: -0.01em;
    margin: 0;
  }
  .zzp-segments-title em { color: var(--accent-primary); font-style: normal; }
  .zzp-segments-lead { line-height: 1.6; color: var(--text-muted, #555); margin: 0; }
  .zzp-segments-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.25rem;
    align-items: stretch;
  }
  .zzp-segment-card {
    background: var(--surface-card, #fff);
    border: 1px solid rgba(0, 0, 0, 0.06);
    border-radius: var(--radius-lg, 14px);
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }
  /* Reset alle margins/indents in de card-children om globale CSS
     niet te laten interfereren (oorzaak van de witte verschuiving eerder). */
  .zzp-segment-card > * {
    margin: 0;
    padding: 0;
    text-indent: 0;
  }
  .zzp-segment-card h3 {
    font-family: var(--font-display, sans-serif);
    font-weight: 800;
    font-size: 1.05rem;
    color: var(--text-primary, #1a1a1a);
  }
  .zzp-segment-body {
    font-size: 0.92rem;
    line-height: 1.55;
    color: var(--text-primary, #1a1a1a);
  }
  .zzp-segment-branches {
    font-size: 0.85rem;
    line-height: 1.5;
    color: #6b7280;
  }
  .zzp-segment-branches strong { color: var(--text-primary, #1a1a1a); font-weight: 600; }

  @media (max-width: 1000px) { .zzp-segments-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 540px) { .zzp-segments-grid { grid-template-columns: 1fr; } }
</style>
