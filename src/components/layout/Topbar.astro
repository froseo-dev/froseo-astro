---
import { pricing } from '@data/site-config';

interface Props {
  text?: string;
}
const {
  text = `Nieuw, AI-powered website vanaf ${pricing.abboFromShort}, inclusief hosting en onderhoud`,
} = Astro.props;
---

<div class="topbar"><span class="star">★</span> {text}</div>
