# Test/build phase — block all crawlers.
# Replace BEFORE going live on froseo.nl with a permissive robots.txt that
# allows indexing of /, /diensten, /werk, /blog, etc. (and Disallow /lab/).
User-agent: *
Disallow: /
