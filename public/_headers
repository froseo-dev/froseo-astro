# Cloudflare Pages — global response headers.
#
# Noindex/nofollow voor de Cloudflare preview (*.pages.dev) gaat via
# functions/_middleware.ts — die zet de X-Robots-Tag header conditioneel
# op basis van hostname. NIET hier in _headers zetten: _headers geldt
# voor elke hostname (inclusief productie) en blokkeert dan ook Google
# op froseo.nl.
